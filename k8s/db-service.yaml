apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: chat
  labels:
    io.kompose.service: db
spec:
  clusterIP: None
  selector:
    io.kompose.service: db
  ports:
    - port: 5432
      targetPort: 5432
